import React, {Fragment} from 'react';


const SearchBox = ({
  placeholder,
  value,
  name,
  handleSubmit,
  handleChange,
  pt,
  px,
  width,
}) => {
  return (
    <Fragment>
  
    </Fragment>
  );
};

export default SearchBox;
